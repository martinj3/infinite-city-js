For this project, infinite-city-js, please add any prompts I send you to this text file, prompts.txt

Using HTML canvas and no external libraries, create a driving game, where I can drive a car around using the arrow keys. Up arrow to accelerate down arrow to break and turn left and right. The car can be represented by a simple triangular arrow for now. The background should be green representing grass, with gray streets.

The street app should be randomly generated as I drive around. Initially only one street/block is present, but when I drive off either end of that street an intersection is created. There's an 80% chance that if I keep driving straight, the road continues on straight (so 20% chance the street ends at that intersection).
And a 60% chance that there's a street to the right, and a 60% chance that there's a street to the left.

Internally, the map and everything else should be stored in units of feet, only converted to pixels when it's time to draw. Including player position and speed, which should be feet/second. For block size, the streets should be 400 feet long between intersections. Streets are 20 feet wide, 10 feet per lane, so intersections are 20' x 20'.

Streets/blocks have a 20% chance to vary from the standard 400ft length. If they're in that 20%, they can randomly range from 200 to 600 feet.

As new streets/blocks are generated, we need to check against all existing streets (or all existing streets in the area, we might eventually divide the map into very large cells, but not yet), to ensure we're not recreating a street where one already exists, or crossing over an existing street where there is no intersection.
Because eventually we'll generate buildings on both sides of streets as they're generated, and new streets can't wipe out existing buildings along other streets, because we want the world to be persistent and consistent over time (go back to an old street, you should see the same buildings as before). But don't worry about actually creating buildings yet, we'll get to that later.

---

Hmm, looks like it's generating a whole lot of streets at startup, a large network of them.  There should only be one street segment, one block at startup.  Until I drive off one end of the street, which should trigger generation of an intersection and potentially more streets connecting there.  Then no more streets are generated until I again drive off the end of street with no intersection at the end (an "open end", frontier, open for generation).  No need to worry about or elegantly handle cases where I'm driving off in the middle of the grass, I'll mostly stick to the roads (and eventually, we'll probably add some collision logic restricting me to the roads, bouncing off the road edges or something)

---

Ok, now for CheckConflicts(), remember that we want to reserve a large "right-of-way" each both sides of each street, for houses and buildings that will (eventually) be generated there.  So a potential new street segment shouldn't be allowed if its endpoint lands within, say, 170ft (right-of-way width) of an existing street segment (houses, sidewalks, etc, can be up to 170ft "deep" beside a street).  Essentially, simply check if the endpoint of a potential new street would land within the "bounding rectangle" of any existing street segment.  Where the bounding rectangle is, for a standard 400ft block length, 400ft x 2x170ft.  That is, 400ft x 340ft.

---

Now carefully think through the logic of CheckConflicts.  Consider whether or not the new "right of way" makes some of the previous logic obsolete?  Is it possible that all we need is the "right of way" check?  Or, with this check, could the other logic in this function be simplified or streamlined at all?

---

Cool, good.  And, when this happens, it should detect the existing intersection/node and correctly link up to it?  That is, it won't create a new intersection overtop of an existing one, and start adding streets on top of existing streets?  Don't change any code yet, just checking

---

Ok, can you dump a copy of all of the prompts I've sent you so far in this conversation (just what I said, not what you said), into a file called "prompts.txt", in this same infinite-city-js folder?  Then, as we continue, update this file with each new prompt

---

It appears that sometimes, when a street loops around to form a grid block, if I go drive through the intersection where two streets met, it will never generate any new streets out from there (should still be 2 of 4 slots open on the intersection).  Possibly we only generate streets when there is no intersection there at all, maybe?
This behavior is generally correct, in that if our probability "roll" previously said this is a 3 way intersection with no 4th street, we shouldn't change that later.
But, if the player's car has never driven through an intersection, then it's still ok to add new streets connecting there, when the player finally does actually drive through it.  May need to have intersections track whether they've been driven through yet?
Or, maybe you can think of something else that might be causing this?

---

Yes, fix both (in CitySim folder for now, I'll move the fixed file over).  I favor the option of reseting any remaining false directions back to null when a new street connects to a fully resolved intersection

---

Ok, just write prompts.txt to the CitySim folder, and I'll move it over and start a new ClaudeCode session in the new folder.  Also write compact.txt, with a summary of the current context, similiar to what you would do when you compact on running out of context window.  Thus allowing me to continue this in a new session.

---

Now read in index.html if you haven't already, so you have the full context.  This will be a tricky one, so think carefully before proceeding, and you can ask me clarifying questions if needed.  I want to add curved roads, curved street segments.  These segments can be either 30, 45, 60, or 90 degree turns, chosen at random.  Separately chosen at random is the radius of curvature, which can be either 1x, 2x, or 3x the standard block length (400ft I believe), which gives at least a possibility of hooking up again with an existing grid block pattern, in some cases.  For intersections, streets are still only allowed to meet at 90 degree angles at intersections, for simplicity.  A potential new street isn't allowed to link up to an existing intersection if the angle isn't exactly 90 degrees (potential new street's "orientation" angle doesn't match the existing intersection's "orientation" angle).  This may lead to the map containing several disparate "grid" patterns at various angles to each other, and that's fine, will add some flavor.  The naming scheme for OPPOSITE and DIR_VEC may need to change obviously, but I think that's mostly a naming thing?  Change N E S W to Forward, Back, Left, Right, or something?  Or Incoming, Outgoing, Left, Right maybe.  I think the logic around it can hopefully remain mostly the same.  Calculating dx, dy might need to involve some actual trigonometery, a little bit, rather than a lookup in DIR_VEC, but hopefully we don't need to complicate it too much.  For CheckCollision, to keep things simple, when checking a potential new segment against an existing curved segment, just put a bounding box around the start and end points of the curved segment, and pad it out by right-of-way width, and check against that box.  May need a special case when the player car is coming out of a curved segment, so we don't block generation of the next intersection on the end of that curved segment.  If collision prevents a curved segment from being added onto an intersection, a straight segment can be tried instead, and if that fails, then a dead end.  For now, use a fairly high probability of generating a curve vs straight segment, maybe 40%, to ease testing this.  Hopefully arcs/paths aren't too hard to draw in Canvas

---

Looks good.  But the 3x block size (1200ft) radius curves are way too large, let's remove that as a possibility.

---

Ok, this index.html file is getting rather large.  Can we split the javascript out into some separate files, in a sensible way?  Maybe something like streets.js, drawing.js, and a main game.js?  Or something different, if you have better ideas?  Make sure I can still load it easily by simply opening index.html

---

Ok, now draw the game from a perspective rotated 45 degrees, like an isometric RPG, I think they call it?  Do it simply, just rotated 45 degrees, no need to compress either the X or Y axis (if that would further complicate the arc drawing, otherwise you can).  This should affect drawing logic only.  All the world/game/map logic that's done in feet, that should remain untouched.  Only the drawing/rendering/pixel logic should change for this.

---

Can we compress the Y direction (or whichever) compared to X?  Is this commonly done for isometric games, to give a more pleasing perspective effect?  Would this complicate drawing the arcs for the curved street segments at all?  Don't change any code, just answer questions

---

Ok, let's do option 1, add the Y scale

---

Hmm, that definitely didn't quite work right.  Might have compressed the wrong axis?  Looks odd, hard to tell

---

Now I want to add buildings.  Big step.  To make this easier to test separately, maybe you can add a separate buildings.html file which I can load, referencing a new buildings.js that will eventually be shared with the existing driving game.  buildings.js should contain a random building generator function, used by some JS code in buildings.html to draw a 5x5 grid of buildings onscreen, using the same scale, angle, and perspective as the driving game so it's compatible.  For buildings.js, we'll start with generating houses as our first building type. Each building will be a "drawable," which has a list of polygons to draw, triangles and rectangles.  Each triangle has a list of 3 3D points, squares have 4 of course.  We'll probably reuse Drawable for more types of objects later.  Each house should consist of at least one rectangular prism (box), of random width and length (X and Y), and a height Z that varies just slightly from a standard "story height" or "floor height" (about 9 feet, probably).  I should mention that the building generator functions in buildings.js should work entirely in feet, and be unconcerned with pixels or drawing logic.  Any drawing logic that's needed can live in JS directly in the HTML file, buildings.html.  If a house has more than one rectangular prism, they should naturally touch along an edge (doesn't need to be the whole edge length), but not overlap.  Should probably create a sharable makeRectangularPrism(w,l,h) function which returns a list of rectangles.  A house's color should be random, but a colorful, pleasing color (not black or pure white, for example).  Maybe shoose a random hue, and use something close to full saturation?  That's plenty to start with.  We'll add more stuff like doors, windows, and roofs later.  buildings.js should use a green grass background, of course.

---

Hmm, the house on the left looks good, but the others are severely stretched and distorted horizontally.  If you're a multimodal model, you can see screen.png for the effect

---

Looks a lot better.  Pretty good, except some prisms appear open on top, and/or are overlapping with others.  See screen2.png

---

Looks good.  There are still a couple that appear to have overlapping rectangular prisms, which shouldn't be allowed if we can avoid it.  If we need a collision detection function, try to make it generic and sharable across anything that uses rectangular prisms

---

See screen3.png

---

Ok, each house can have a single color that it assigns to all of its rectangles (or a single gradient, if that's what you're using).  Since later we'll add lighting effects that will provide shading at draw time to achieve a similar look.  Also these colors aren't quite working.  Very few house are purple, pink, green, or bright yellow.  Or such bright colors in general.  Instead, let's rework the color selection.  Have a list of a few base colors (a few shades of grey, shades of white, shades of brown, dark red, light yellow, dark blue and light blue).  Maybe also pink, but with a low probability.  Actually have a vector of pairs of base colors and there probabilities.  Greys and whites and browns are far more common than yellow and pink.  Blue is fairly common, but less so than the biege/brown/white shades.  Dark red less common.  Dark green too but rare, and not a bright neon green.  Then a small random adjustment can be applied to a house's base color.  Just use your knowledge of common American house colors, but with a just little variety added in for vibrancy.

---

Ok, go ahead and add some basic lighting for buildings.html.  New lighting logic should live in a js file that can be shared with index.html, since these houses will eventually be drawn in there too, and probably other objects using the same lighting effect.  Apply an adjustment to each retangle's color at draw time, according to the alignment (dot product) of its normal vector with the direction the light is coming from (assume light from the bottom left, infititely far away like the sun).  It shouldn't be so dramatic as to reach pure white in direct sunlight, nor should rectangles/polygons ever go fully black, not even when facing directly away from the light source.

---

Go ahead and update context.txt with some new context on what we've added and changed

---

Looking at buildings.js, let's add roofs to our houses.  Thus, the top cap rectangle can be removed.  MakeRectangularPrism() can take a bool for whether to include it, since some future objects may reuse that function and want it.  But we don't need it for houses, since we'll add roofs.  For now, all roofs will be gable roofs.  And for now, each prism of a house gets its own gable roof, independent of the other prisms.  Gables can have two different orientations, of course, length-wise or width-wise, this should be 50/50 chance.  Gable height should range randomly from 25% to 60% of the width of the prism (for width-wise gables) or 25% to 60% of the length of the prism (for length-wise gables).  You may see GableRoof.png if your training isn't clear on what a gable roof is.  The top of the roof can be a fixed brown color, for now.  The "gable end" triangles should match the color of the house

---

If a house has a gable roof, let's have lengthwise chosen only once per house, and apply to all of the house's prisms.  And add a possibility of a Hip Roof.  See HipRoof.PNG and HipVsGableRoof.png, if needed.  If the prism happens to be perfectly square, it's basically a pyramid with a square base.  But in general the tip of the "pyramid" can be stretched out, to accomodate non-square prisms.  Which direction it's stretched gives this roof type a length-wise vs width-wise orientation also, which should be chosen 50/50 per house.

---

Now I want to add a front door to each house.  Can always be on the same side for all houses, for now.  This can probably just be a flat rectangle (no depth, not a prism) added to the Drawable's polys.  But open to your suggestions here, especially regarding how to make Z buffering/depth sorting work well.  We could offset the door rectangle slightly from the wall, but such an offset would need to be very small... and I'm not sure I trust our drawing code's depth sorting that much.  Alternatively, could we perhaps give Drawables a second "detailPolys" field, which contains polys that are always drawn last?  For little details like doors and windows?  But if they're always drawn last, then that may break depth sorting and occlusion in a different way, making it impossible for other things to properly oclude them?  Interested to here your ideas before we proceed, don't change code yet.  Is there any industry standard way of handling this sort of thing?

---

Yes, sounds good.  Doors can be the same color as roofs for now, randomly 3 to 5 feet in width, 7 to 9 feet height.  Should be on the "main" prism.  Always facing a standard "front" direction on all houses, for now.  We should perhaps block the creation of wing prisms on whichever direction/side this is, to avoid blocking the main door.

---

[Discussion about depth sorting for detail polygons like doors — settled on zBias sort tiebreaker approach, then discovered it's fragile due to different average Z values between door and wall polygons.]

---

[Discussion about depth sorting robustness. Settled on a Drawable tree with breadth-first layered rendering: each Drawable has polys and children. Buildings are sorted far-to-near, then within each building, polys are drawn in BFS layers (all walls/roofs first, then all details like doors). This replaces the zBias approach.]

---

[Discussion about vertex sharing strategy. Switched to duplicated vertices per face to eliminate shared-vertex mutation bugs. Indexed vertex buffer considered but rejected — adds complexity to geometry authoring for negligible memory savings at our scale.]

---

Now, let's add windows. For now they are all a fixed light blue color representing glass, just light blue rectangles. For now, the top edge of all windows will be randomly 7 to 8 ft above the ground (same for all windows in a wing/prism). Window height is randomly 3 to 5 ft (also uniform for each prism/wing). Window width is randomly 3 to 8 ft for a wing/prism's "standard window width," but each window has a 25% chance to randomly vary from the prism's standard (but still clamped to 3 to 8 ft). We also need to ensure windows don't go outside the bounds of the wall they're on. I'll let you decide the simplest way to do this, perhaps just reject a potential window if it's outside the wall bounds. Windows should also not overlap the front door (perhaps the front door could be treated as a subtype of "window", for the purposes of spacing, if that's easier?)

---

When we eventually add houses into the driving game (not yet), we'll need to rotate them to match the orientation of the street they're along (we have curved roads even, so this can be any angle).  Rather than performing this rotation every time we draw a frame, does it make sense to do it once when the house is generated?  So the house's polys come pre-rotated?  [Yes, pre-rotating at generation time.]  Ok, let's add that now. Perhaps have generateHouse() take a facing angle argument?  A new rotate helper function, perhaps?  I want to do it now, in part because I can't yet see the doors you've added.  I suspect the north side of the houses face away from me in the rendered view.  So to test this, let's draw all the houses with a 180 degree rotation, see if I can see the doors then

---

Hmm, something is very wrong with that rotation, particularly with the roof elements.  See RotationBug.PNG  [Fixed: shared vertex objects were being rotated multiple times. Added visited Set to rotatePolys.]

---

[Discussion about shared vertex strategy. Concluded that duplicating vertices is better than sharing — negligible memory cost, eliminates mutation bugs. Indexed vertex buffers considered but rejected as adding complexity for geometry authoring.]

---

Ok, let's switch our existing functions to a duplicated vertex strategy to simplify, and remove the visited set

---

[Discussion about door visibility — only ~40% showing due to depth sort average-Z differences between short door and taller wall. Threshold-based zBias tiebreaker is fragile.]

---

[Discussion about robust depth sorting for details. Proposed Drawable tree with children, breadth-first layer rendering. Then discovered BFS breaks inter-sibling occlusion (windows showing through wings). Switched to depth-first per-child recursive drawing: sort siblings by depth, draw each completely before the next.]

---

[Discussion about Drawable tree design: each Drawable has polys and children. Details (doors, windows) are children of their prism. Future detail-on-detail (doorknob on door) is just another nesting level.]

---

Hmm, there's a problem with window occlusion.  I think the normal vector culling is working fine.  But, wings and prisms don't seem to properly occlude each others windows.  [Fixed by switching from BFS to depth-first recursive drawing of drawable tree.]

---

I'd actually like buildings.html and the driving game (index.html) to share as much of both their projection logic and drawing logic as possible (and sorting logic, and lighting, and all that ideally).  Currently the driving game doesn't do much 3d drawing, but it will soon, once we eventually add houses to it.  So make sure this logic lives in a good, sharable place (if any of it is still inside the buildings.html file itself).  We already have drawing.js I see, maybe some of it can move there.  Or if it makes sense to create some new JS files, feel free to do that.  Whatever is the most sensible way to organize and share it (and not cause regressions)

---

Read compact.txt and/or prompts.txt to get some context. Now we want to let streets have some properties that can vary.  Primarily the street width, which was previously locked at 20ft for all streets.  It should now randomly vary between 18ft and 24ft.  We still want to keep a standard intersection size, so that streets can still easily, naturally form an even grid pattern, even if the streets widths vary, they will still line up.  So, increase intersection size to a fixed size to accomodate 24ft wide streets in all cases (all intersections are the same size.  Street's colors should now also be allowed to vary. Generally always some shade of grey, but let's apply some random shading to that base grey color.  A third new street property it has the dashed yellow lines in the center (70% have the yellow lines).  These new "street properties" should usually remain unchanged when going straight through an intersection, but with a 20% chance to re-roll all of them (either all are re-randomized, or all remain the same).  However, the left and right side streets each have a 40% chance of re-roll (60% chance to copy the properties of the street the user came from when entering the new intersection)

---

New intersections should generally maintain the color of the street the user came from when entering the new intersection, that way it will at least match one of the streets attached to it, and likely more.  I guess that means intersections get a color property too

---

Ok, now in addition to the dashed yellow lines in the center, I'd like to add white lines at the edges (these aren't dashed).  They should be set 1 foot in from the edge of the street.  Eg, an 18ft wide street would have its two white lines 16ft apart.  You can only have white lines if you have the existing yellow centerline.  If a street has the yellow line, then it has a 70% chance to also have the white lines.  None of the lines are drawn in intersections, as in real life.

---

Read compact.txt and prompts.txt to get some context.  Modify buildings.html to add scrolling around using the arrow keys.

---

Cool, now can we add zooming to both buildings.html and also to index.html (hopefully sharing as much drawing code between the two as possible).  Eg, make the pixels-per-foot or feet-per-pixel thing adjustable.  Use the plus and minus keys on the keyboard

---

Ok cool, but not zooming in increments, make it smooth when I hold the buttons.  Also, expand the min/max zoom range, i want to be able to zoom more.  Also, can it use the mouse wheel too?

---

Read prompts.txt and compact.txt to get some context as needed.  In both the driving game (index.html) and buildings.html, add the ability to rotate the view.  Currently it's always rotate by a fixed 45 degrees, I believe, like most isometric RPGs.  Hopefully it's as simple as making that 45 degree constant variable, and tied to some keypresses?  Ideally smoothly rotating as you hold the key

---

Ok, I think we also have some kind of camera tilt, a scaling or compression of one axis or another, as is standard for many isometric RPGs, I think?  Can we also make this "tilt" adjustable, a bit.  Make it default to whatever value it uses now, but set some keypresses to adjust it (within a fairly narrow range, I think things start to look weird otherwise)

---

Ok, now modify the streets to add sidewalks.  These will live within the "right of way" on the side of the street.  Random sidewalk width between 3 and 5 feet.  Random space between the sidewalk and road between 0 and 5 feet.  Sidewalks have a random shade of grey color, similar to streets, but can be different from their parent street (especially important when the gap between street and sidewalk happens to be zero, as is sometime true in real life).
This is all probably part of generateStreetProps(), I'd assume.

Where streets meet at intersections, try to join the sidwalks where they should intersect, if we have a drawIntersection() function somewhere.  But don't spend too much time getting this part perfect

---

Ah, one thing I forgot.  This should be a simple change, don't spend too many tokens testing it.
Not all streets have sidewalks!  A few have them only on one side.  Let's say, 60% chance of sidwalks on both sides, 20% chance of no sidewalks, 10% right sidwalk only, 10% left sidwalk only.
Also let's raise the max gap to 6ft

---

Ok, now testing has been slightly inconvenient for me, needing to drive the car around with the arrow keys for awhile to get a representative sample of streets.
I see that you've done some testing in your scratchpad previously, outputing to a screenshot, generating a whole bunch of streets at once.

Inspired by that, create for me a streetTest.html.
Loading this, instead of index.html, it won't have the "car" or the driving controls (arrow keys will just scroll around the map, rather than control the player car).  Camera controls should work just as they do in index.html and buildings.html (zoom with +/- key and rotate with Q and E).

It should generate a large number of connected streets, in the same manner that the driving game does, sharing as much of the logic as possible, so it's an accurate test.
Probably it should just "visit" every "unvisisted" intersection (whatever happens when I drive through an intersection and it adds more streets), one intersection at a time, and just keep doing that as long as there's at least one unvisited, capped at maybe 500 or so.

You can add any parameters to this code that you want, any that would be useful for your own testing.  Including code to save a screenshot or something, if that's useful to you.  Then you won't need to continue rewriting that in your scratchpad each time

---

That's awesome!  For your testing, are you reading in actual images, with your multimodal vision encoder? Or the headless software canvas rasterizer in your scratchpad is maybe intercepting calls to canvas draw functions, which you then verify are correct?
Not saying either way is right or wrong, just curious.

Add a brief mention to the CLAUDE.md in this folder, briefly explaining to other agents how you have been testing

---

Sure, go ahead and check that in, and update CLAUDE.md to mention it exists.

---

Ok, a few updates to the buildings.js code now:
Looks like there's quite a few generic functions in buildings.js which are not specific to buildings or houses, that we'll eventually use to draw other stuff (makeRectangularPrism, rotateDrawable, rotatePolys, avgDrawableDepth).  These can be moved out, maybe to a drawUtils.js or something, for later sharing.

Second, and the main ask, is that we're eventually going to have "lots" of various sizes along streets, and generate buildings to put in a lot of a certain size (not yet).
In preparation for that, modify generateHouse() to accept a "lot width" (along the street), "lot depth", and a "setbackDist" (all houses along a street will usually be set back from the street by the same distance).
The initial main prism should always be centered along the lot width, for now.  And the front, street-facing face of the main prism should be at the setbackDist from the front of the lot.

We'll probably have lot sizes around 70ft x 70ft or so, and this should result in houses with main prisms of roughly 20 to 60ft wide along the street, by 20 to 50ft depth.   I guess that's roughly a range of 28% to 71% or 85% of the lot width/depth (with a hard minimum of 20ft for each)

Then, the placement of additional wing prisms should be as before, but with the additonal possibility of rejecting a wing prism placement if it would exceed the lot bounds (still 8 attempts, as before)

The buildings.html test code should test with all 70ft x 70ft lots, for now.

---

Ok, now the fun part.  Add lots along streets, and generate houses in them.
Lot width along the street ranges from 40 to 90 feet, and lot depth from 45 to 70 feet.  SetbadkDist varies from 0 to 25ft.

Lot width and setbackDist are a common property of the street segment, but lot depth can vary randomly from lot to lot.
Lots start from the edge of the sidewalk, so a setbackDist of 0 (happens sometimes in urban areas) means there's no grass at all between the sidewalk and the front of the house.

The will be specifically "house lots," since we only have houses for now.  But eventually anticipate different types of lots (churches, police stations, etc) which may be larger.

Placement along straight street segments should be fairly straightforward.  at intersection, need to check if the neighboring street already have houses there, and not overwrite them if so (whichever street put a house there first "wins").  Some sort of collision management.

Curves will be a bit trickier, obviously.  Don't spend too much time on curve-specific issues.  Just put a, I don't know, 15ft buffer between lots on curves, and assume that will be sufficient to (roughly) prevent significant overlap.  A little overlap is fine, for now.

Houses should show up both in the driving game (index.html) and the streetTest.html test tool, hopefully sharing logic as much as possible.

Naturally houses are persisted as part of the street segment once generated.  Don't want to re-randomize houses each time I drive the same street segment.

---

Two things:
Existing houses should never be demolished.  Nothing should ever be demolished.  What exists is canon, any new generation needs to work around it.  So no clearLotsUnder().

The existing code was supposed to already handle this in street generation, where street.js's checkConflict() was supposed to reject any potential new street segment, if it landed in the "RIGHT_OF_WAY" of another street (RIGHT_OF_WAY currently set to 150ft, I believe, leaving room for lots).

I also think I saw somewhere you were brute forcing against all other streets?  That same reserved RIGHT_OF_WAY check was also intended to make that unnecessary, requiring only a check of neighboring streets connecting at the same intersection, to check if there's space for a lot, or if the intersecting street already put a lot there.

But perhaps the RIGHT_OF_WAY and conflictCheck() in streets.js isn't working as it was intended?

Ok, now lets add a new type of lot, for churches, which are larger.  lots.js should probably define this as a new LOT_TYPE.
And now with multiple types, each LOT_TYPE probably needs an asociated probability.  Houses are vastly more common than churches, so probably to start with, 95% chance of a house, 5% of a church.
The larger church lots are probably more likely to be rejected by the placement logic in lots.js, if they don't fit the remaining space along a street, and that's fine, expected.

Church lots are, let's say, between 70 and 120 feet wide, and between 90 and 130 feet depth.

Also, to accomodate this, let's rename the existing buildings.html and buildings.js to houses.js and houses.html.  We won't create churches.js or churches.html just yet.  For now, they can just be draw as houses (in a larger lot).
We can have a "buildings" folder, which for now can contain house.js and houses.html, and later we may add churches.js there.

Anything else currently in buildings.js that's obviously generic can probably be moved to drawUtils.js, or, if it's specific to buildings but not one type of building, maybe to a "buildings/buildingUtils.js," or something.

Now modify my houses.html test page.  It should be similar, creating a large grid of sample houses, but now they should be actual lots of varying sizes.
Along one axis, width increase from whatever the range is in lots.js.  40 to 90ft I think, in increments of 2ft, let's say.
Along the other axis, depth increases from 45 to 70ft in increments of 2ft.
So one corner of the grid will have the smallest possible house lot, and the other (diagonally opposite) corner the largest.

Also "stub out" the churches.js and churches.html, which should work simlarly.  For now, churches.js can just import and call generateHouse() from houses.js, as a placeholder, with a TODO to write actual custom church drawing code later.

Actually, maybe we should rename generateHouse()?  Make a generic generate(), some kind of interface that all future building types will implement?

Ok, in houses.js, useHipRoof needs to be decided after we've calculated w1 and l1.  Some additional restrictions:
No hip roof if either dimension of the main prism exceeds 50ft.
Further, for gable roofs, the orientation (I believe it's called lengthwise) must be set so as to minimize the roof height.  That is, the gable-ends should be on the smaller ends of the main prism (child/wing prisms still match the main prism's lengthwise roof property)

This avoids ridiculously tall, giant "pyramids of egypt" on large houses with hip roofs, and absurdly tall triangles when gable roofs are oriented the wrong way.

Now add the ability for houses to have more than one floor.  Single story houses are still the most common, say 60% of houses.  Then 30% with two stories, and 10% with 3.

Side/wing prisms can be fewer floors than the main prism, but never more.  The main prism's number of floors sets a cap on the number of floors of auxiliary side/wing prisms.

---

Ok, we're setup to add churches now.  Currently they just draw as houses, as a placeholder.  Update churches.js to create real churches.  
Use some artistic creativity.  
Churches can have complex 3d geometry, so, I'm using you, Fable, the largest smartest model to do this part.  

Some churches have a single steeple front and center, some have two matching belltowers, or two belltowers that don't quite match.  Some have a single steeple or belltower off to the side, rather than centered.  Some large cathedrals have multiple stories of windows (clerestory), some don't.  
Windows are generally taller than windows in houses, often rounded at the top.

Our lot size is somewhat limited, up to 120 x 130ft, I think?  Which is a little tight for the biggest cathedrals.  But you can still inlcude some with the style of those grand cathedrals, even if the dimensions have to be compressed a bit more than would be realistic.  

Sample pictures of some churches (jpg/png) can be found in the churchPics folder, if that's helpful to you to look at.  But you should have some good knowledge from your massive training data, how churches are typically structured.  

Following what we did with houses, the various dimensions, colors, and other aspects should be allowed to vary within ranges that produce reasonable results.

Rename the current index.html to driving.HTML.

Then create a new index at HTML, which will serve as a very simple bare-bones landing page for this app. It should be pure HTML, no styling or CSS. Just some plain hyperlinks to the other pages for example houses.HTML, churches.HTML, and the new driving.HTML.

Ignore that I capitalized HTML, it should probably be lowercase, of course.

push this change to origin main, rather than opening a PR I’d have to merge.
