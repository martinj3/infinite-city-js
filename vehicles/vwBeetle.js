// --- VW Beetle (placeholder) ---
// No body style of its own yet -- draws as a sedan (see registerPlaceholderVehicle()
// in vehicleUtils.js) until it gets one. Weight 0, so it can never be the player's
// car or, later, turn up in traffic.
registerPlaceholderVehicle('vwBeetle');
